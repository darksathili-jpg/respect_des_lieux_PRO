{
  "name": "respect-des-lieux-pro",
  "version": "5.2.1-alpha.6",
  "private": true,
  "description": "Application desktop locale de suivi des dégradations en établissement scolaire — données stockées uniquement sur le poste utilisateur.",
  "main": "main-entry.cjs",
  "author": "Lycée Watteau",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=24.21.0"
  },
  "scripts": {
    "start": "electron .",
    "check": "node --check main-entry.cjs && node --check main-bootstrap.cjs && node --check main.cjs && node --check preload.cjs && node --check src/database.cjs && node --check src/reparation-edit-extension.cjs && node --check src/storage.cjs && node --check src/portable-backup.cjs && node --check renderer/app.js && node --check renderer/detail.js && node --check scripts/ux-guardrails.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && node scripts/quality-contract.mjs --role all && node scripts/ux-guardrails.mjs && npm test",
    "dist:win:dir": "electron-builder --win dir --x64 --publish never",
    "dist:win": "electron-builder --win nsis --x64 --publish never"
  },
  "devDependencies": {
    "electron": "44.4.0",
    "electron-builder": "26.16.1"
  },
  "build": {
    "appId": "fr.lycee-watteau.respect-des-lieux-pro",
    "productName": "Respect des Lieux PRO",
    "artifactName": "Respect-des-Lieux-PRO-${version}-${arch}.${ext}",
    "asar": true,
    "electronFuses": {
      "runAsNode": false,
      "enableCookieEncryption": true,
      "enableNodeOptionsEnvironmentVariable": false,
      "enableNodeCliInspectArguments": false,
      "enableEmbeddedAsarIntegrityValidation": true,
      "onlyLoadAppFromAsar": true,
      "loadBrowserProcessSpecificV8Snapshot": false,
      "grantFileProtocolExtraPrivileges": true
    },
    "files": [
      "**/*",
      "!tests/**",
      "!docs/**",
      "!.github/**",
      "!design/**",
      "!quality/**",
      "!scripts/**",
      "!README.md"
    ],
    "win": {
      "target": ["nsis"]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    }
  }
}